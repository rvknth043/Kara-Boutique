'use client';

// This page reuses the same form component as new address
// The form detects edit mode based on URL parameter

import AddressFormPage from '../../new/page';

export default AddressFormPage;
